import React from "react";

const Teams = () => {
  const style = {
    marginBottom: "130px",
    color: "white",
    "text-align": "center",
  };

  return (
    <div>
      <h1 style={style}>
        Here we're going to have information about every team
      </h1>
    </div>
  );
};

export default Teams;
